# Copy to .env and fill in from your Supabase project settings (API section).
# The anon key is public by design and safe in client code.
# Never put the service_role key here - it bypasses row level security.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Set to 1 to render generated nights instead of the database, for working on
# the chart without data behind it.
# VITE_DEMO=1
